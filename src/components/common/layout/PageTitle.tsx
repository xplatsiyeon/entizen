/** @format */

const PageTitle = ({ title }: { title: string }) => {
  return <h1 className='text-4xl text-center uppercase font-bold font-Pretendard mo:mt-14 sm:text-2xl sm:pt-20'>{title}</h1>;
};
export default PageTitle;
